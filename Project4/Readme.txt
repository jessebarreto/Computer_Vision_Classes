

Para compilar o projeto em um ambiente Unix com o OpenCV é necessário o uso do Cmake instalado no Sistema Operacional.

Para compilar basta utilizar os seguintes comandos:

$ cmake .
$ make

Com isso os binários estarão presentes na pasta do código-fonte.

ou

Execute o script buildall.sh.